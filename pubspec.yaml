name: esp32_stream_detection
description: ESP32-CAM stream with ML Kit object detection

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_mlkit_object_detection: ^0.13.0
  path_provider: ^2.1.0

flutter:
  uses-material-design: true
